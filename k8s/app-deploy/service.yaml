apiVersion: v1
kind: Service
metadata:
  labels:
    app: full-stack-k8s
  name: full-stack-k8s-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: full-stack-k8s
  type: NodePort
